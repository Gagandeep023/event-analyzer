/**
 * The analysis engine.
 *
 * Pure functions, zero dependencies, no I/O. Events in, results out. That
 * constraint is what makes this module trivially testable and usable standalone
 * against an array from any database, with no server involved.
 *
 * Implemented in phase 2. See docs/06-core-engine.md.
 */

export const CORE_VERSION = '0.1.0';
